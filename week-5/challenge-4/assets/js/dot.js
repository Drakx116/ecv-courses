class Dot {
    constructor() {
        this.pos = { x: 0, y: 0 }
        this.color = COLOR.DEFAULT;
        this.radius = RADIUS.DEFAULT;
        this.type = TYPE.DEFAULT;
        this.points = POINTS.DEFAULT;
    }
}
